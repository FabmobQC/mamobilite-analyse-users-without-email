# mamobilite-analyse-users-without-email
Analyse the issue of users without email

## Installation
``` sh
pip install -r requirements.txt
```

## Configuration
The config file (conf.json) has one mandatory fields:
- "db_url": The url of the mongodb database.

## Run
``` sh
python main.py
```